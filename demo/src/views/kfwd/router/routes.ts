import { Routes, getRoute } from "@/router"
const routes: Routes = {}
export const xtm = 'kfwd'
getRoute(routes, xtm, 'ZUi')
getRoute(routes, xtm, 'git')
getRoute(routes, xtm, 'rule')
export default routes